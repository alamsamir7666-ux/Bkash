# Force npm to install devDependencies even when NODE_ENV=production.
#
# Render sets NODE_ENV=production automatically (or it's set as an env
# var), which makes `npm install` skip devDependencies. But our build
# step needs @types/* packages (declared as devDependencies) to compile
# TypeScript. This .npmrc forces them to always be installed.
include=dev
